services:
  swagger-agent:
    build: .
    env_file: .env
    volumes:
      - swagger-cache:/app/.cache
      - ./outputs:/app/outputs
      - ${TARGET_DIR:-.}:/work
    entrypoint: ["python", "-m", "swagger_agent", "/work"]

volumes:
  swagger-cache:
