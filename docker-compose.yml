services:
  swagger-agent:
    build: .
    volumes:
      - swagger-cache:/app/.cache
      - ./outputs:/app/outputs
      - ./.env:/app/.env:ro
      - ${TARGET_DIR:-.}:/work
    entrypoint: ["python", "-m", "swagger_agent", "/work"]

volumes:
  swagger-cache:
